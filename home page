<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Playa Personal Training – Playa del Carmen</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --sky: #0ea5e9;
      --sky-dark: #0284c7;
      --dark: #0f172a;
      --light: #f8fafc;
      --mid: #64748b;
      --border: #e2e8f0;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--light);
      color: var(--dark);
      font-size: 16px;
      line-height: 1.6;
    }

    /* ── NAV ── */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1.1rem 5%;
      background: rgba(15,23,42,0.95);
      backdrop-filter: blur(8px);
    }

    .nav-logo {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.5rem;
      color: #fff;
      letter-spacing: 0.05em;
    }

    .nav-logo span { color: var(--sky); }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      color: rgba(255,255,255,0.75);
      text-decoration: none;
      font-size: 0.85rem;
      font-weight: 500;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      transition: color 0.2s;
    }

    .nav-links a:hover { color: var(--sky); }

    /* ── HERO ── */
    #hero {
      min-height: 100svh;
      background: var(--dark);
      display: flex;
      align-items: center;
      position: relative;
      overflow: hidden;
      padding: 7rem 5% 5rem;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      background:
        radial-gradient(ellipse 70% 60% at 80% 50%, rgba(14,165,233,0.18) 0%, transparent 65%),
        radial-gradient(ellipse 40% 40% at 10% 90%, rgba(14,165,233,0.08) 0%, transparent 60%);
    }

    .hero-stripe {
      position: absolute;
      top: 0; right: 0;
      width: 4px;
      height: 100%;
      background: linear-gradient(to bottom, transparent, var(--sky), transparent);
      opacity: 0.5;
    }

    .hero-content {
      position: relative;
      max-width: 700px;
    }

    .hero-tag {
      display: inline-block;
      background: rgba(14,165,233,0.15);
      color: var(--sky);
      font-size: 0.78rem;
      font-weight: 500;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      padding: 0.35rem 0.9rem;
      border-radius: 2rem;
      margin-bottom: 1.5rem;
      border: 1px solid rgba(14,165,233,0.3);
    }

    .hero-content h1 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(3.2rem, 8vw, 6.5rem);
      line-height: 0.95;
      color: #fff;
      letter-spacing: 0.02em;
      margin-bottom: 1.5rem;
    }

    .hero-content h1 em {
      font-style: normal;
      color: var(--sky);
    }

    .hero-content p {
      font-size: 1.1rem;
      color: rgba(255,255,255,0.65);
      max-width: 480px;
      margin-bottom: 2.5rem;
      font-weight: 300;
    }

    .btn {
      display: inline-block;
      background: var(--sky);
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.9rem;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      padding: 0.9rem 2.4rem;
      border-radius: 3px;
      transition: background 0.2s, transform 0.15s;
    }

    .btn:hover {
      background: var(--sky-dark);
      transform: translateY(-2px);
    }

    .btn-outline {
      background: transparent;
      border: 1.5px solid rgba(255,255,255,0.3);
      margin-left: 1rem;
    }

    .btn-outline:hover {
      background: rgba(255,255,255,0.08);
      border-color: rgba(255,255,255,0.6);
    }

    /* ── SECTION COMMONS ── */
    section { padding: 6rem 5%; }

    .section-label {
      display: inline-block;
      font-size: 0.75rem;
      font-weight: 500;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--sky);
      margin-bottom: 0.75rem;
    }

    .section-title {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(2.2rem, 5vw, 3.5rem);
      line-height: 1.05;
      letter-spacing: 0.02em;
      margin-bottom: 1rem;
    }

    .section-sub {
      font-size: 1rem;
      color: var(--mid);
      max-width: 540px;
      font-weight: 300;
    }

    /* ── SERVICES ── */
    #services { background: var(--dark); color: #fff; }

    #services .section-sub { color: rgba(255,255,255,0.5); }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1.5px;
      margin-top: 3.5rem;
      background: rgba(255,255,255,0.07);
      border: 1.5px solid rgba(255,255,255,0.07);
    }

    .service-card {
      background: var(--dark);
      padding: 2.5rem 2rem;
      position: relative;
      overflow: hidden;
      transition: background 0.25s;
    }

    .service-card::before {
      content: '';
      position: absolute;
      bottom: 0; left: 0;
      width: 100%; height: 3px;
      background: var(--sky);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.3s;
    }

    .service-card:hover { background: #141e33; }
    .service-card:hover::before { transform: scaleX(1); }

    .service-icon {
      width: 44px; height: 44px;
      background: rgba(14,165,233,0.12);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.4rem;
    }

    .service-icon svg {
      width: 22px; height: 22px;
      stroke: var(--sky);
      fill: none;
      stroke-width: 1.8;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .service-card h3 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.55rem;
      letter-spacing: 0.04em;
      margin-bottom: 0.75rem;
      color: #fff;
    }

    .service-card p {
      color: rgba(255,255,255,0.55);
      font-size: 0.93rem;
      font-weight: 300;
      line-height: 1.65;
    }

    /* ── ABOUT ── */
    #about {
      background: var(--light);
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 5rem;
      align-items: center;
    }

    .about-visual {
      position: relative;
    }

    .about-box {
      background: var(--sky);
      aspect-ratio: 4/5;
      border-radius: 4px;
      position: relative;
      overflow: hidden;
    }

    .about-box::after {
      content: '';
      position: absolute;
      inset: 0;
      background: repeating-linear-gradient(
        45deg,
        rgba(255,255,255,0.04) 0px,
        rgba(255,255,255,0.04) 1px,
        transparent 1px,
        transparent 20px
      );
    }

    .about-stat {
      position: absolute;
      bottom: -1.5rem;
      right: -1.5rem;
      background: var(--dark);
      color: #fff;
      padding: 1.4rem 1.8rem;
      border-radius: 4px;
      text-align: center;
    }

    .about-stat strong {
      display: block;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 2.5rem;
      color: var(--sky);
      line-height: 1;
    }

    .about-stat span {
      font-size: 0.78rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: rgba(255,255,255,0.5);
    }

    .about-text .section-title { color: var(--dark); }
    .about-text p {
      color: var(--mid);
      font-size: 1rem;
      font-weight: 300;
      line-height: 1.8;
      margin-bottom: 1rem;
    }

    .about-text p:last-of-type { margin-bottom: 0; }

    /* ── TESTIMONIALS ── */
    #testimonials { background: var(--dark); color: #fff; }
    #testimonials .section-sub { color: rgba(255,255,255,0.5); }

    .testi-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1.5rem;
      margin-top: 3.5rem;
    }

    .testi-card {
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 4px;
      padding: 2rem;
      position: relative;
    }

    .testi-card::before {
      content: '\201C';
      font-family: Georgia, serif;
      font-size: 4rem;
      color: var(--sky);
      line-height: 1;
      display: block;
      margin-bottom: 0.5rem;
      opacity: 0.7;
    }

    .testi-card p {
      font-size: 0.97rem;
      color: rgba(255,255,255,0.7);
      font-weight: 300;
      line-height: 1.75;
      margin-bottom: 1.5rem;
    }

    .testi-author {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .testi-avatar {
      width: 38px; height: 38px;
      border-radius: 50%;
      background: var(--sky);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1rem;
      color: #fff;
      flex-shrink: 0;
    }

    .testi-name {
      font-weight: 500;
      font-size: 0.9rem;
      color: #fff;
    }

    .testi-loc {
      font-size: 0.78rem;
      color: rgba(255,255,255,0.4);
    }

    /* ── CONTACT ── */
    #contact { background: var(--light); }

    .contact-inner {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 5rem;
      align-items: start;
    }

    .contact-text .section-title { color: var(--dark); }
    .contact-text p {
      color: var(--mid);
      font-weight: 300;
      font-size: 1rem;
      line-height: 1.8;
      margin-bottom: 2rem;
    }

    .contact-links { display: flex; flex-direction: column; gap: 1rem; }

    .contact-link {
      display: flex;
      align-items: center;
      gap: 1rem;
      text-decoration: none;
      color: var(--dark);
      padding: 1.1rem 1.4rem;
      border: 1.5px solid var(--border);
      border-radius: 4px;
      background: #fff;
      font-weight: 500;
      transition: border-color 0.2s, box-shadow 0.2s;
    }

    .contact-link:hover {
      border-color: var(--sky);
      box-shadow: 0 0 0 3px rgba(14,165,233,0.1);
    }

    .contact-link-icon {
      width: 38px; height: 38px;
      background: rgba(14,165,233,0.1);
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .contact-link-icon svg {
      width: 18px; height: 18px;
      stroke: var(--sky);
      fill: none;
      stroke-width: 1.8;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .contact-link span { font-size: 0.93rem; }

    .contact-callout {
      background: var(--dark);
      border-radius: 6px;
      padding: 2.5rem;
      color: #fff;
    }

    .contact-callout h3 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.8rem;
      letter-spacing: 0.04em;
      margin-bottom: 0.5rem;
    }

    .contact-callout p {
      color: rgba(255,255,255,0.55);
      font-size: 0.93rem;
      font-weight: 300;
      line-height: 1.7;
      margin-bottom: 1.5rem;
    }

    .contact-callout .btn { width: 100%; text-align: center; display: block; }

    .location-line {
      display: flex;
      align-items: center;
      gap: 0.4rem;
      font-size: 0.82rem;
      color: rgba(255,255,255,0.35);
      margin-top: 1.5rem;
    }

    .location-line svg {
      width: 14px; height: 14px;
      stroke: var(--sky);
      fill: none;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
      flex-shrink: 0;
    }

    /* ── FOOTER ── */
    footer {
      background: #080d18;
      color: rgba(255,255,255,0.3);
      text-align: center;
      padding: 1.8rem 5%;
      font-size: 0.82rem;
    }

    footer span { color: var(--sky); }

    /* ── RESPONSIVE ── */
    @media (max-width: 768px) {
      nav .nav-links { display: none; }

      #about {
        grid-template-columns: 1fr;
        gap: 3rem;
      }

      .about-box { aspect-ratio: 3/2; }

      .about-stat {
        right: 1rem;
        bottom: -1rem;
      }

      .contact-inner {
        grid-template-columns: 1fr;
        gap: 3rem;
      }

      .hero-content h1 { font-size: clamp(2.8rem, 11vw, 4rem); }

      section { padding: 4.5rem 5%; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="nav-logo">Playa<span>PT</span></div>
  <ul class="nav-links">
    <li><a href="#services">Services</a></li>
    <li><a href="#about">About</a></li>
    <li><a href="#testimonials">Results</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-bg"></div>
  <div class="hero-stripe"></div>
  <div class="hero-content">
    <div class="hero-tag">📍 Playa del Carmen, Mexico</div>
    <h1>Train Hard.<br /><em>Live Better.</em></h1>
    <p>1-on-1 personal training and custom workout plans built for your goals. Real results, right here in paradise.</p>
    <a href="#contact" class="btn">Start Training</a>
    <a href="#services" class="btn btn-outline">See Services</a>
  </div>
</section>

<!-- SERVICES -->
<section id="services">
  <div class="section-label">What I Offer</div>
  <div class="section-title">Services</div>
  <p class="section-sub">Every session is tailored to you — no cookie-cutter programs, no wasted effort.</p>

  <div class="services-grid">
    <div class="service-card">
      <div class="service-icon">
        <svg viewBox="0 0 24 24"><path d="M6 4v16M18 4v16M2 12h20M2 7h4M18 7h4M2 17h4M18 17h4"/></svg>
      </div>
      <h3>1-on-1 Coaching</h3>
      <p>Dedicated personal training sessions focused entirely on your goals, form, and progress. Train smarter, not harder.</p>
    </div>
    <div class="service-card">
      <div class="service-icon">
        <svg viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M9 3v18M3 9h6M3 15h6"/></svg>
      </div>
      <h3>Custom Workout Plans</h3>
      <p>Personalized programs designed around your lifestyle, schedule, and equipment — gym-based or outdoor sessions.</p>
    </div>
    <div class="service-card">
      <div class="service-icon">
        <svg viewBox="0 0 24 24"><path d="M12 2a10 10 0 1 0 0 20A10 10 0 0 0 12 2z"/><path d="M12 6v6l4 2"/></svg>
      </div>
      <h3>Nutrition Guidance</h3>
      <p>Simple, sustainable eating habits that complement your training and help you look and feel your best year-round.</p>
    </div>
  </div>
</section>

<!-- ABOUT -->
<section id="about">
  <div class="about-visual">
    <div class="about-box"></div>
    <div class="about-stat">
      <strong>8+</strong>
      <span>Years<br/>Experience</span>
    </div>
  </div>
  <div class="about-text">
    <div class="section-label">About Me</div>
    <div class="section-title">Coaching<br/>with Purpose</div>
    <p>I'm a certified personal trainer based in Playa del Carmen with a passion for helping people unlock their full potential. Whether you're a beginner just starting out or an athlete pushing for new records, I'm here to guide every step.</p>
    <p>I believe great fitness is built on consistency, smart programming, and genuine support. My approach cuts through the noise so you can focus on what actually moves the needle — and enjoy the journey along the way.</p>
  </div>
</section>

<!-- TESTIMONIALS -->
<section id="testimonials">
  <div class="section-label">Client Results</div>
  <div class="section-title">What People Say</div>
  <p class="section-sub">Real words from real clients who committed to the process.</p>

  <div class="testi-grid">
    <div class="testi-card">
      <p>I lost 14 kg in four months and finally feel strong. The custom plan made all the difference — it actually fit my life instead of asking me to change everything overnight.</p>
      <div class="testi-author">
        <div class="testi-avatar">SR</div>
        <div>
          <div class="testi-name">Sofia R.</div>
          <div class="testi-loc">PDC, Mexico</div>
        </div>
      </div>
    </div>
    <div class="testi-card">
      <p>Best investment I've made for my health. The sessions are challenging but fun, and the results showed up faster than I ever expected. Highly recommend to anyone serious about change.</p>
      <div class="testi-author">
        <div class="testi-avatar">JM</div>
        <div>
          <div class="testi-name">James M.</div>
          <div class="testi-loc">Toronto, Canada</div>
        </div>
      </div>
    </div>
    <div class="testi-card">
      <p>As a remote worker living in Playa, I needed a trainer who could work around my schedule. The flexibility and professionalism here are second to none. My energy levels have completely transformed.</p>
      <div class="testi-author">
        <div class="testi-avatar">AL</div>
        <div>
          <div class="testi-name">Anna L.</div>
          <div class="testi-loc">Berlin, Germany</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact">
  <div class="contact-inner">
    <div class="contact-text">
      <div class="section-label">Get In Touch</div>
      <div class="section-title">Ready to<br/>Start?</div>
      <p>Send a message or give me a call to discuss your goals and find out how we can work together. First consultation is free.</p>
      <div class="contact-links">
        <a href="tel:+520000000000" class="contact-link">
          <div class="contact-link-icon">
            <svg viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2A19.79 19.79 0 0 1 11.37 19a19.5 19.5 0 0 1-5-5A19.79 19.79 0 0 1 3.1 4.17 2 2 0 0 1 5.09 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L9.09 9.91A16 16 0 0 0 14.09 15l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
          </div>
          <span>+52 000 000 0000</span>
        </a>
        <a href="mailto:hello@example.com" class="contact-link">
          <div class="contact-link-icon">
            <svg viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
          </div>
          <span>hello@example.com</span>
        </a>
      </div>
    </div>

    <div class="contact-callout">
      <h3>Book a Free Consult</h3>
      <p>Not sure where to start? Let's talk for 20 minutes about your goals, your lifestyle, and how training can fit into your world in Playa.</p>
      <a href="tel:+520000000000" class="btn">Call Now</a>
      <div class="location-line">
        <svg viewBox="0 0 24 24"><path d="M21 10c0 7-9 13-9 13S3 17 3 10a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
        Playa del Carmen, Quintana Roo, Mexico
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <p>© 2025 <span>Playa Personal Training</span> · Playa del Carmen, Mexico · All rights reserved.</p>
</footer>

</body>
</html>
